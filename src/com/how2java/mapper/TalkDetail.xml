<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.how2java.mapper.TalkDetailMapper">
	    
<!-- 	            加载楼层 -->
		<select id="listdetail"    resultType="TalkDetail">
			SELECT *FROM talkdetail_ Where talkid = ${talkid} order by sort asc
		</select>

<!-- 		寻找帖子id -->
		<select id="get" parameterType="Integer"    resultType="TalkDetail">
			SELECT talkid  FROM talkdetail_   WHERE sort=(SELECT MAX(sort) FROM talkdetail_)
		</select>
	</mapper>